import { StyleSheet, Text, View } from "react-native";
import React from "react";

const WorkingProofs = () => {
  return (
    <View>
      <Text>WorkingProofs</Text>
    </View>
  );
};

export default WorkingProofs;

const styles = StyleSheet.create({});
