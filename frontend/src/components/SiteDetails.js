import { StyleSheet, Text, View } from "react-native";
import React from "react";

const SiteDetails = () => {
  return (
    <View>
      <Text>SiteDetails</Text>
    </View>
  );
};

export default SiteDetails;

const styles = StyleSheet.create({});
